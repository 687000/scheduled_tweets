<h1>Edit Scheduled Tweets</h1>
<%= render "form", tweet: @tweet %>
<%= form_with model: @tweet, data: { turbo: false } do |form| %>
    <% if form.object.persisted? %>
        <%= button_to "Delete", tweet_path(@tweet), method: :delete, class: "btn btn-outline-danger"%>
    <% end %>
<% end %>